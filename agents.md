# Agent Instructions
- The `references/` directory is read-only: inspect it for architectural inspiration, but never modify its contents.
- When designing or coding, capture the distilled architecture in `architecture_reference.md` so the implementation relies on that document rather than touching `references/` again.
